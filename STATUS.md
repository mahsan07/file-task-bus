# Status

- Stage: runnable MVP (`0.1.0`)
- Implementation: task schema, atomic claims, lifecycle lanes, approvals, failures, watcher, digest, CLI, and Python API
- Verification: 5 local unit tests plus reproducible Python and PowerShell demos
- Runtime: Python 3.11+, standard library only
- Known boundary: local filesystems; no distributed lock or hosted service guarantee
